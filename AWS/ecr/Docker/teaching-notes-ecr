Using the docker file provided, build a docker image and run it.

docker build -t demo-app .
docker run -it <ContainerID>

AUthenticate to ECR and push the file.(Check view push commands for the commands)

aws ecr get-login-password --region us-east-1



